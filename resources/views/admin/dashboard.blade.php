@extends('layouts.app')
@section('title','Admin Dashboard | '.$site['name'])
@section('content')
<section class="mx-auto max-w-7xl px-4 py-12"><div class="flex flex-wrap items-center justify-between gap-4"><div><p class="text-sm font-semibold uppercase tracking-[0.16em] text-gold">Internal admin</p><h1 class="mt-2 text-4xl font-semibold text-navy">Dashboard</h1></div><form method="POST" action="{{ route('admin.logout') }}">@csrf<button class="rounded border border-slate-200 bg-white px-4 py-2">Logout</button></form></div><div class="mt-8 grid gap-4 md:grid-cols-4">@foreach($stats as $label=>$value)<div class="rounded border border-slate-200 bg-white p-5"><p class="text-sm text-steel">{{ $label }}</p><p class="text-2xl font-semibold">{{ $value }}</p></div>@endforeach</div><h2 class="mt-10 text-2xl font-semibold text-navy">CMS Modules</h2><div class="mt-5 grid gap-4 md:grid-cols-3 lg:grid-cols-4"><a href="{{ route('admin.leads.index') }}" class="rounded border border-slate-200 bg-white p-5"><p class="font-semibold">Leads</p><p class="mt-1 text-xs text-steel">Review, search and manage website enquiries.</p></a>@foreach($contentModules as $slug=>$definition)<a href="{{ route('admin.content.index',$slug) }}" class="rounded border border-slate-200 bg-white p-5"><p class="font-semibold">{{ $definition['label'] }}</p><p class="mt-1 text-xs text-steel">Authenticated content records.</p></a>@endforeach</div></section>
@endsection
